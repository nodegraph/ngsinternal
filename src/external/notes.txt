
freetype-2.5.2
-- build the debug and release outputs
-- you'll to look a bit around for the built outputs

glew-1.1.3.0
-- build the debug MX and release MX outputs

glm
-- just unpack this and it's ready to go

freetype-gl
-- use cmake-gui
-- comment out the following lines from CMakeLists.txt

#IF ( NOT freetype-gl_LIBS_SUPPLIED )
#    FIND_PACKAGE( glfw3 REQUIRED )
#ENDIF ( NOT freetype-gl_LIBS_SUPPLIED )

-- in cmake-gui, uncheck all the output byproducts 
   like the api docs, demos, harfbuzz, makefont
-- check the freetype-gl_USE_VAO to use vertex array objects

freetype-gl on Anroid
-- on android comment out the following lines from CMakeLists.txt

#if(NOT APPLE)
#    find_package(GLEW REQUIRED)
#endif()
...
include_directories(
    ${OPENGL_INCLUDE_DIRS}
    ${FREETYPE_INCLUDE_DIRS}
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${VS789FIX_INCLUDE_DIR}
    #${GLEW_INCLUDE_PATH}
)

-- modify freetype-gl/opengl.h

#else
//#  include <GL/glew.h>
//#  include <GL/gl.h>
    #include <GLES3/gl3.h>
    #include <GLES3/gl3ext.h>
#endif

